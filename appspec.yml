version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/html
hooks:
 ApplicationStart:
    - source: /home/ec2-user/.bash_profile
    - location: appstart.sh
      timeout: 300
      runas: root
